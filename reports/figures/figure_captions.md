# Figure Captions

Các biểu đồ sử dụng giá trị trung bình từ benchmark local. Chúng không đại diện cho hiệu năng production.

- **write_latency_comparison.png**: So sánh thời gian trung bình cho thao tác `write` giữa Parquet fallback và Iceberg table trên cùng dữ liệu và cùng Spark engine.
- **read_latency_comparison.png**: So sánh thời gian trung bình cho thao tác `read_count` giữa Parquet fallback và Iceberg table trên cùng dữ liệu và cùng Spark engine.
- **query_latency_comparison.png**: So sánh thời gian trung bình cho thao tác `query` giữa Parquet fallback và Iceberg table trên cùng dữ liệu và cùng Spark engine.